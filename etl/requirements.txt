# Python dependencies for the etl/src helper scripts.
#   pip install -r etl/requirements.txt   (or: uv pip install -r etl/requirements.txt)
#
# pypdf is REQUIRED (not optional): generate_manuals.py aborts without it, because
# the "> 1 page" check is what rejects 1-page Declaration-of-Conformity stubs.
pypdf>=6.13.2
# databricks-sdk backs the Knowledge Assistant scripts (w.knowledge_assistants).
# Pinned to the version this work was verified against.
databricks-sdk>=0.126.0
